Usage: main.py [options]

Options:
  -h, --help            show this help message and exit
  -v, --verbose
  -d DEST_DIR, --dest_dir=DEST_DIR
                        Directory where support bundle should be extracted.
  -l LOG, --log=LOG     Path to log
  -b BUG, --bug_id=BUG Bug id
  -c, --clear-old       Clear the old data
  -n HOST, --host=HOST  ELK hostname or ip address
  -k KIBANA_PORT, --kibana_port=KIBANA_PORT
                        Kibana port
  -e ES_PORT, --es_port=ES_PORT
                        Elasticsearch port



How to run? 
Set and environment variable ELK_REPO to the root directory of this repo.
export ELK_REPO="/path/to/first-responder"

Sample command:
$ main.py --log <log_path> --dest_dir <dest_dir> --host 10.39.1.211 --bug_id 12345 --verbose

Confluence page for Event summarizer: https://confluence.eng.vmware.com/pages/viewpage.action?spaceKey=NSBU&title=CCP+Event+Summarizer+Design
Confluence page for ELK: https://confluence.eng.vmware.com/pages/viewpage.action?pageId=681951289