Usage: main.py [options]

Options:
  -h, --help            show this help message and exit
  -v, --verbose
  -d DEST_DIR, --dest_dir=DEST_DIR
                        Directory where support bundle should be extracted.
  -l LOG, --log=LOG     Path to log



Sample command:
$ python main.py --log <log_path> --dest_dir <dest_dir> --verbose
